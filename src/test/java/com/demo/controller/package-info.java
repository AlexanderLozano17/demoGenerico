/**
 * Paquete que contiene las clases de prueba para los controladores REST.
 * 
 * Esta clase contiene pruebas unitarias e integradas para los controladores
 * que manejan las solicitudes HTTP y las respuestas en la aplicación.
 * Las pruebas verifican la correcta gestión de las rutas, la validación de entrada
 * y la respuesta adecuada a las solicitudes.
 * 
 */

package com.demo.controller;