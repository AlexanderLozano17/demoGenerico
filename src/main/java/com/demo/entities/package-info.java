/**
 * Este paquete contiene las entidades del dominio que representan las tablas y objetos del modelo de datos
 * en la base de datos. Las clases dentro de este paquete están estrechamente asociadas con el modelo relacional 
 * y son utilizadas por las capas superiores para persistir y recuperar datos.
 *
 * <p>Las entidades implementan los mapeos a través de frameworks como JPA o Hibernate, y siguen las convenciones 
 * estándar para garantizar la consistencia y la integridad de los datos en la base de datos.</p>
 *
 * <p>Se recomienda que las entidades sean lo más simples posible, manteniendo solo las propiedades que 
 * reflejan directamente el modelo de datos y evitando la lógica de negocio dentro de estas clases.</p>
 *
 * @since 1.0
 */

package com.demo.entities;

